{
  "name": "gymbt-rules-tests",
  "private": true,
  "scripts": {
    "test": "jest --runInBand",
    "test:emulator": "cd .. && npx firebase-tools emulators:exec --only firestore,storage \"npm test --prefix rules-tests -- --runInBand\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.14.1",
    "jest": "^29.7.0"
  }
}
